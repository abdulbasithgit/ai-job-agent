# Copy this file to .env and fill in values. Never commit .env.

NODE_ENV=development
LOG_LEVEL=info

# Path to the user profile JSON (skills, locations, preferred roles)
USER_PROFILE_PATH=./config/profile.json

# ---- Added in later phases (kept here so you see where we are going) ----
# AI_PROVIDER=mock            # mock | openai | ollama (Phase 4)
# OPENAI_API_KEY=             # Phase 4
# DATABASE_URL=               # Phase 6
# SMTP_HOST=                  # Phase 7
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASSWORD=
# EMAIL_FROM=
# EMAIL_TO=
# JOB_AGENT_CRON=0 9 * * *    # Phase 8
